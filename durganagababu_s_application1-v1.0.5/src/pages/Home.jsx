import React from "react";
import HomePagePage from "./HomePage"; // Assuming HomePage is in the same directory

const Home = () => {
    return (
        <div>
            <HomePagePage />
        </div>
    );
};

export default Home;
